export class TownUpgrade {
    constructor(scene, level, cost, entity, text) {
        this.scene = scene;
        this.level = level;
        this.cost = cost;
        this.entity = entity;
        this.text = text;
    }
}